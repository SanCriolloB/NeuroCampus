# --- Runtime mínimo para la API (Producción) ---

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Validación (Pydantic v2)
pydantic>=2.7

# Subidas de archivos (FormData / UploadFile)
python-multipart>=0.0.9

# Procesamiento de datos (lectura CSV/XLSX/Parquet)
pandas>=2.2
pyarrow>=15.0
openpyxl>=3.1

# Utilidades base (compatibilidad según entorno)
numpy>=1.26
typing-extensions>=4.8
