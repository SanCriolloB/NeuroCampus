/**
 * Componente raíz.
 * Día 1: "pantalla vacía" con texto placeholder.
 * 
 * Notas:
 * - Aquí luego montaremos Router, Layouts y páginas reales.
 * - Usamos inline styles mínimos para no añadir dependencias CSS.
 */
export default function App() {
  return (
    <div style={{ padding: 16 }}>
      <h1>NeuroCampus — UI (Día 1)</h1>
      <p>Frontend inicial con Vite + React + TypeScript.</p>
    </div>
  );
}