/**
 * ResultsTable — Componente placeholder para mostrar resultados/tablas.
 * Día 3+: reemplazar por una tabla real (paginación/ordenamiento).
 */
export default function ResultsTable(){
  return <div className="card">ResultsTable — TODO</div>
}