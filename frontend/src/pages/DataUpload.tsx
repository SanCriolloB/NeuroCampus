/**
 * DataUpload — Página "vacía" del Día 1.
 * Propósito:
 * - Aquí irá el flujo de carga de datos (Día 2).
 * - Mantener sin dependencias externas por ahora.
 */
export default function DataUpload(){
  return <div className="mono">DataUpload — TODO UI</div>
}