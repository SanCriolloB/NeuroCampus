/**
 * Models — Página "vacía" del Día 1.
 * Propósito:
 * - Listado/gestión de modelos (Días 4–5).
 * - Placeholder para contratos iniciales.
 */
export default function Models(){
  return <div className="mono">Models — TODO UI</div>
}