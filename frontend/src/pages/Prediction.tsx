/**
 * Prediction — Página "vacía" del Día 1.
 * Propósito:
 * - UI para predicción online/batch (Día 6).
 * - Mantener solo el placeholder hoy.
 */
export default function Prediction(){
  return <div className="mono">Prediction — TODO UI</div>
}