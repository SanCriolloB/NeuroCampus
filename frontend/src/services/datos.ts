/**
 * servicios/datos — Día 2 definirá contratos reales con el backend.
 * Aquí solo dejamos "stubs" con tipos para no romper el build.
 */
export type UploadResult = { ok: boolean; id?: string; error?: string }
export const placeholder = true